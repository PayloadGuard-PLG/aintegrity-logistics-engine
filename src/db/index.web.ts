// Web stub — expo-sqlite is JSI-only and unavailable on web.
// Metro resolves this file instead of index.ts when bundling for web.

export const useDbMigration = () => ({ success: true as const, error: undefined });
